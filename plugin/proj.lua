-- Public entrypoint. Sourced automatically by Neovim when the plugin is on
-- runtimepath. Calls setup() with empty opts so the plugin is usable without
-- explicit setup in user config.


require("proj").setup({})
